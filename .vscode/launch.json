{
  "configurations": [
    {
      "name": "Docker Node.js Launch",
      "type": "docker",
      "request": "launch",
      "preLaunchTask": "docker-run: debug",
      "platform": "node",
      "node": {
        "package": "${workspaceFolder}/comsentimento_front_test/package.json",
        "localRoot": "${workspaceFolder}/comsentimento_front_test"
      }
    }
  ]
}
